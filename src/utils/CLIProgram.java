package utils;

public class CLIProgram {



    /*private static void payBills(String[] command, billRepository, paymentService) {

        List<Bill> bills = new ArrayList<>();

        for (int i = 1; i < command.length; i++) {

            Bill bill = billRepository.findById(command[i]);

            if (bill == null) {

                System.out.println("Sorry! Not found a bill with such id");
                return;
            }

            bills.add(bill);
        }

        paymentService.payMultipleBills(customer, bills);

        System.out.println("Your current balance is: " + customer.getBalance());
    }*/
}
